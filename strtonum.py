#Timothy A. Gibbons
#strtonum
#
import time; 

def main():
	impt = input("Enter phase: ")
	impt = impt.split(' ');
	val = 0;
	for i in impt:
		for j in range(len(str(i))):
			val = chr(ord(ch)+key);
	print(val);
	time.sleep(600);
main();
